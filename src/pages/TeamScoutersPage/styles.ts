import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },

    playerList: {
        marginTop: -30,
    },

    
});

export default styles;